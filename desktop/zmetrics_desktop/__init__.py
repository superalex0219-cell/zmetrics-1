"""ZMetrics desktop client (Python + PySide6, Windows-first).

The single ZMetrics client: a thin REST consumer of the FastAPI backend plus local
stereo capture (ZED 2 as UVC) and an offline SQLite queue. CV runs server-side.
"""

__version__ = "0.1.0"
